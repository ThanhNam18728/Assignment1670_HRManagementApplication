
@model IEnumerable<HRManagement.Models.Category>
@{
    ViewBag.Title = "Index";
}

<h2>Manage Category</h2>
<h3 class="btn btn-default" style="margin-bottom: 7px;">@Html.ActionLink("Create Category", "Create", "Categories")</h3>

<div class="form-group">
    @using (Html.BeginForm("Index", "Categories", FormMethod.Get))
    {
        @Html.TextBox("searchString", "")
        <button type="submit"><i class="fa fa-search"></i></button>
    }
</div>

<table class="table table-bordered">
    <thead>
        <tr>
            <td>#</td>
            <td>Name</td>
            <td>Description</td>
            <td>Actions</td>
        </tr>

    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>
                    @Html.ActionLink("Details", "Details", "Categories", new { id = item.Id }, new { @class = "btn btn-primary" })
                    @Html.ActionLink("Edit", "Edit", "Categories", new { id = item.Id }, new { @class = "btn btn-primary" })
                    @Html.ActionLink("Delete", "Delete", "Categories", new { id = item.Id }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </tbody>
</table>

